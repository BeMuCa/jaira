---
phase: quick-260813-0z0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/view.go
  - internal/tui/model.go
  - internal/tui/render_test.go
autonomous: true
requirements: [260813-0z0]

must_haves:
  truths:
    - "The detail pane shows the full 26-character ULID as selectable text, not only the 6-character handle."
    - "Pressing y in the detail pane copies the full ULID to the system clipboard."
    - "After pressing y the detail pane shows a visible confirmation that the copy happened."
    - "The confirmation disappears on the next keypress — it is transient, not sticky state."
    - "The help screen (?) and the detail footer hint both list y."
  artifacts:
    - path: "internal/tui/view.go"
      provides: "id row in renderDetail, copied marker, footer hint, help entry"
      contains: "t.ID"
    - path: "internal/tui/model.go"
      provides: "y binding in the modeDetail key handler returning tea.SetClipboard, plus the copied flag on Model"
      contains: "tea.SetClipboard"
    - path: "internal/tui/render_test.go"
      provides: "test that the detail pane prints the full id and that y yields a clipboard command"
  key_links:
    - from: "internal/tui/model.go (modeDetail case)"
      to: "bubbletea SetClipboard"
      via: "returned tea.Cmd"
      pattern: "tea\\.SetClipboard\\(m\\.detail\\.ID\\)"
    - from: "internal/tui/model.go (m.copied)"
      to: "internal/tui/view.go (renderDetail id row)"
      via: "Model field read during render"
      pattern: "m\\.copied"
---

<objective>
The TUI only ever shows the 6-character handle. Anyone who needs to hand a ticket to
the CLI or to another agent has to go find the full ULID on disk. Show it in the
detail pane, and bind `y` to put it on the clipboard.

Purpose: close the gap between "I am looking at this ticket" and "I can name this
ticket to something else".
Output: one new row in the detail pane, one new key, one new test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@internal/tui/view.go
@internal/tui/model.go
@internal/tui/render_test.go
@core/ticket/id.go
</context>

<verified_context>
Established by reading the code — do not re-derive, but do read the surrounding
lines before editing so the change matches local style.

- `renderDetail` is `internal/tui/view.go:446-511`. Its header line (452) renders
  `ticket.Handle(t.ID)` + title. The field block below it is built by the local
  `row(k, v string)` closure (455-460), which skips empty values and wraps at
  `min(m.width-14, 64)` with a 13-column hanging indent.
- `row` already accepts a value with a styled suffix appended: the `lane` row does
  exactly this at 462 and 464 (`t.Status + styAsks.Render("  ▲ ...")`). Reuse that
  shape for the copied marker instead of inventing a new rendering path.
- The detail footer hint is `view.go:508-509`:
  `"e fields · E body · m lane · jk next/prev · esc back"`, truncated to
  `min(m.width, 78)`.
- The help screen is `renderHelp`, `view.go:562-612`. Its sections are
  "Move around", "Look at things" (577-580), "Change things" (581-590). Key labels
  are padded with `%-9s`.
- Detail-mode keys are handled at `internal/tui/model.go:486-514`, inside
  `func (m *Model) key(k tea.KeyPressMsg) (tea.Model, tea.Cmd)` (374). The
  `case modeDetail:` block ends `return m, nil` (514) — returning a real `tea.Cmd`
  from here is already supported by the signature. `y` is unbound.
- `tea.SetClipboard(s string) Cmd` exists in bubbletea v2.0.8
  (`clipboard.go:28-34`, OSC52). No new dependency. `tea` is already imported by
  `model.go` (`tea.Quit`).
- `m.notify(msg, isErr)` (`model.go:590-593`) sets `m.mode = modeMessage`, which
  takes over the whole screen and closes the detail pane. That is the wrong weight
  for a copy confirmation — do not use it here.
- `styOK` exists (`view.go:38`).
- There are no golden/snapshot files in this repo (`find` for `*.golden` and
  `testdata/` returns nothing). The detail-pane tests are contains-based:
  `checklist_test.go:90` and `:120` call `stripANSI(m.renderDetail())` and assert on
  substrings. **An added row therefore breaks no snapshot.**
  `TestChecklistLinesDoNotOverflow` (checklist_test.go:115-131) only measures lines
  it identifies as checklist lines, so the new id row is outside its assertion — and
  the test's own comment already documents that other parts of the detail pane
  overflow at narrow widths, so a 26-character ULID at width 20 is pre-existing,
  accepted behaviour and must not trigger a rewrite of `row`/`wrap`.
- Tests drive keys directly: `h.Update(tea.KeyPressMsg{Code: 'j', Text: "j"})`
  (`home_test.go:74`). `newTestModel(t, w, h)` is `render_test.go:75-84`.
</verified_context>

<tasks>

<task type="auto">
  <name>Task 1: Show the full id in the detail pane and bind y to copy it</name>
  <files>internal/tui/view.go, internal/tui/model.go</files>
  <action>
In `internal/tui/model.go`, add a single boolean field to `Model` next to the other
detail-pane state (near `detail`, model.go:66-68), named `copied`, with a comment
explaining WHY it exists — OSC52 gives no feedback, so the pane has to say the copy
happened — and that it is transient. Do not add a timer, a new message type, or a
new mode; the marker is cleared by the next keypress.

In the `case modeDetail:` block (model.go:486-514), clear `m.copied = false` once at
the top of the block, before the `switch s`, so any key dismisses the marker. Add a
`case "y":` that sets `m.copied = true` and returns `m, tea.SetClipboard(t.ID)` for
the currently open ticket — read it from `m.detail` and guard against nil the same
way the surrounding code does. Copy the full `t.ID`, never `ticket.Handle(t.ID)`:
the point is a value the CLI accepts unambiguously.

In `internal/tui/view.go` `renderDetail`, add an `id` row as the first `row(...)`
call, above the existing `lane` row, so the pane leads with identity and the header
stays uncrowded. When `m.copied` is set, append a styled suffix to that row's value
using the same shape the `lane` row already uses — `styOK.Render("  copied")`.

Update the footer hint string (view.go:509) to include the new key, keeping the
existing separator style, e.g. `"e fields · E body · y copy id · m lane · jk next/prev · esc back"`.

Add `y` to `renderHelp`'s "Look at things" section (view.go:577-580) — it is a read
action, not a mutation — with a one-line description naming that it copies the full
id and that it works in the detail pane.

Do not enable mouse support, do not add a dependency, do not restructure
`renderDetail`, `row`, or `wrap`.
  </action>
  <verify>
    <automated>export PATH=$PATH:$HOME/.local/go/bin && go build ./... && go vet ./... && go test ./internal/tui/... 2>&1 | tail -20</automated>
  </verify>
  <done>The build and vet are clean, the existing tui tests still pass, `renderDetail` emits the full `t.ID`, `y` in detail mode returns a non-nil clipboard command, and both the footer hint and the help screen list `y`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test the id row, the copy key and the transient marker</name>
  <files>internal/tui/render_test.go</files>
  <behavior>
    - The detail pane for a loaded ticket contains the ticket's full ID, not only its handle.
    - Sending `y` (`tea.KeyPressMsg{Code: 'y', Text: "y"}`) in `modeDetail` returns a non-nil `tea.Cmd` and leaves the mode as `modeDetail` (the pane must not close).
    - After `y`, `stripANSI(m.renderDetail())` contains the confirmation word.
    - After a subsequent unrelated keypress the confirmation is gone.
  </behavior>
  <action>
Add one test function to `internal/tui/render_test.go`, built with
`newTestModel(t, 150, 32)` and a detail ticket set the way the existing detail tests
do. Assert the four behaviours above. Drive keys through `m.Update(tea.KeyPressMsg{...})`,
matching `home_test.go:74`. Do not assert on the clipboard contents themselves —
OSC52 writes to the terminal, not to anything a unit test can read back; assert that
a command was returned and let that stand for the wiring. Write the test's doc
comment in the repo's voice: why the behaviour matters, not what the lines do.
  </action>
  <verify>
    <automated>export PATH=$PATH:$HOME/.local/go/bin && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <done>`go test ./...` passes with the new test present, and the new test fails if the `y` binding or the id row is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI → terminal emulator | OSC52 escape sequence carries the ticket id out of the process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0z0-01 | Information disclosure | `tea.SetClipboard(t.ID)` | accept | A ULID is a non-secret identifier the CLI already prints; the copy is user-initiated by an explicit keypress |
| T-0z0-02 | Tampering | dependencies | mitigate | No package installs — `tea.SetClipboard` ships in the already-vendored bubbletea v2.0.8 |
</threat_model>

<verification>
export PATH=$PATH:$HOME/.local/go/bin && go build ./... && go vet ./... && go test ./...

Manual (optional, not a gate): run `jaira`, open a ticket, confirm the id row is
present and selectable, press `y`, confirm the marker appears and clears on the next
key. OSC52 is not supported by every terminal — the printed id row is the fallback,
which is why it is not optional.
</verification>

<success_criteria>
- `go build ./...`, `go vet ./...` and `go test ./...` all pass.
- The detail pane prints the full ULID.
- `y` in the detail pane returns a `tea.SetClipboard` command carrying the full ULID.
- The confirmation marker appears on `y` and is gone after the next keypress.
- `?` lists `y`, and so does the detail footer hint.
- Diff touches only `internal/tui/view.go`, `internal/tui/model.go` and
  `internal/tui/render_test.go`. No new dependency, no mouse support, no new mode.
</success_criteria>

<output>
Create `.planning/quick/260813-0z0-show-and-copy-the-full-ticket-id-in-the-/260813-0z0-SUMMARY.md` when done
</output>
