# Quick 260813-0z0: Show and copy the full ticket id in the detail pane — Summary

Detail pane now leads with the full 26-character ULID, `y` puts it on the clipboard
via `tea.SetClipboard` (OSC52), and a transient "copied" marker confirms the write
and clears on the next keypress. Footer hint and help screen both list `y`.

## Commits

- `2575334` — feat: show the full ticket id in the detail pane and bind y to copy it
  (`internal/tui/model.go`, `internal/tui/view.go`)
- `94995fe` — test: cover the detail id row, the y copy key and the transient marker
  (`internal/tui/render_test.go`)

## Deviations from Plan

None — plan executed exactly as written.

## Final Verification

```
$ export PATH=$PATH:$HOME/.local/go/bin && go build ./... && go vet ./... && go test ./...
?   	github.com/BeMuCa/jaira/cmd/jaira	[no test files]
ok  	github.com/BeMuCa/jaira/core/board	(cached)
ok  	github.com/BeMuCa/jaira/core/gate	(cached)
?   	github.com/BeMuCa/jaira/core/gitrepo	[no test files]
?   	github.com/BeMuCa/jaira/core/lane	[no test files]
ok  	github.com/BeMuCa/jaira/core/merge	(cached)
ok  	github.com/BeMuCa/jaira/core/project	(cached)
?   	github.com/BeMuCa/jaira/core/session	[no test files]
ok  	github.com/BeMuCa/jaira/core/ticket	(cached)
ok  	github.com/BeMuCa/jaira/core/validate	(cached)
ok  	github.com/BeMuCa/jaira/internal/cli	(cached)
ok  	github.com/BeMuCa/jaira/internal/tui	(cached)
?   	github.com/BeMuCa/jaira/scripts/iconpreview	[no test files]
?   	github.com/BeMuCa/jaira/scripts/shotgen	[no test files]
```

## Self-Check

- `git log --oneline --all | grep 2575334` — FOUND
- `git log --oneline --all | grep 94995fe` — FOUND
- `internal/tui/model.go` contains `case "y":` and `tea.SetClipboard(m.detail.ID)` — FOUND
- `internal/tui/view.go` contains the `id` row, updated footer hint, and the new
  help entry — FOUND
- `internal/tui/render_test.go` contains `TestDetailShowsAndCopiesFullID` — FOUND
- Regression check: temporarily renamed the `"y"` case to `"ynope"` and re-ran the
  new test — it failed (`y did not return a clipboard command`), confirming the
  test is load-bearing. Reverted via `git checkout -- internal/tui/model.go`
  before the final commit.

Diff touches only `internal/tui/view.go`, `internal/tui/model.go` and
`internal/tui/render_test.go`. No new dependency, no mouse support, no new mode.

## Self-Check: PASSED
